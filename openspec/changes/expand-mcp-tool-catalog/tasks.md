# Tasks

## Tasks

- [ ] Model tool catalog as extensible configuration (schemas + metadata per tool)
- [ ] Add authorization requirement metadata to each catalogued tool
- [ ] Enforce per-tool authorization in the ExecuteTool use case before invoking the ACL
- [ ] Add version fields to tool contracts and a versioning policy document
- [ ] Register additional ERP tools beyond `erp.create_order` / `erp.cancel_invoice`
- [ ] Add tests for catalog extensibility, per-tool authorization, and version coexistence
